#ifndef BINDERTOOLS_H
#define BINDERTOOLS_H

#include <QObject>

class BinderTools : public QObject
{
    Q_OBJECT
public:
    explicit BinderTools(QObject *parent = nullptr);

signals:

public slots:
};

#endif // BINDERTOOLS_H